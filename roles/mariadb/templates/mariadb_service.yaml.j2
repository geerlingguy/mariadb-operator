---
apiVersion: v1
kind: Service
metadata:
  name: '{{ meta.name }}'
  namespace: '{{ meta.namespace }}'
  labels:
    app: mariadb
    managed-by: ansible
spec:
  ports:
    - port: 3306
  selector:
    app: mariadb
    tier: database
