---
apiVersion: v1
kind: Service
metadata:
  name: '{{ meta.name }}'
  namespace: '{{ meta.namespace }}'
  labels:
    app: mariadb
spec:
  ports:
    - port: 3306
  clusterIP: None
  selector:
    app: mariadb
